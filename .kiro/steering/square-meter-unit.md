---
inclusion: always
---

# Unidad de metros cuadrados

En todo texto visible para el usuario, representa los metros cuadrados exactamente como `mt2`.

Conserva exactamente como `m2` todas las claves, propiedades, campos, nombres de variables, identificadores internos y contratos existentes. No renombres ni migres identificadores internos de `m2` a `mt2`.
